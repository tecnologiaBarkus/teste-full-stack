{
  "name": "challengepokemon",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "start": "nodemon dist/server.js",
    "watch-ts": "tsc -w",
    "dev": "concurrently -k -p \"[{name}]\" -n \"Typescript,Node\" \"npm run watch-ts\" \"npm run start\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "analuramalho",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "typescript": "^4.2.2"
  },
  "devDependencies": {
    "concurrently": "^6.0.0",
    "nodemon": "^2.0.7"
  }
}
